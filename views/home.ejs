<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("components/boilerplate") %>
        <link rel="stylesheet" href="/css/home.css">
    </head>
<body>
    <img src="/COMPSA Logo.png" alt="COMPSA Logo" id="compsa-logo">
    <h1>COMPSA Scavenger Hunt!</h1>
    <h2>Winners</h2>
    <hr style="width: 60%;">
    <ol>
        <% for( let winner of winners ) { %>
            <div class="winner">
                <li><%= winner.name %></li>
                <p style="text-align: left;">TeamID: <%= winner.id %></p>
                <% const opt = { month: "long", day: "numeric", hour: "numeric", minute: "numeric"} %>
                <p style="text-align: left;">Time: <%= winner.createdAt.toLocaleString("en-US", opt) %></p>
            </div>
        <% } %>
    </ol>
</body>
</html>