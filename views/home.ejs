<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("components/boilerplate") %>
        <link rel="stylesheet" href="/css/home.css">
    </head>
<body>
    <img src="/COMPSA Logo.png" alt="COMPSA Logo" id="compsa-logo">
    <h1>COMPSA Scavenger Hunt!</h1>

    <h2 style="color: rgb(255, 150, 150)">Event is Over</h2>
    <a href="https://github.com/yangavin/scavenger-hunt"
    target="_blank"
    style="color: unset; margin-bottom: 3rem;">
        Check the repository for more info
    </a>

    <h2>Winners</h2>
    <hr style="width: 60%;">
    <ol>
        <% for( let winner of winners ) { %>
            <div class="winner">
                <li><%= winner.name %></li>
                <p style="text-align: left;">TeamID: <%= winner.id %></p>
                <% const opt = { month: "long", day: "numeric", hour: "numeric", minute: "numeric"} %>
                <p style="text-align: left;">Time: <%= winner.createdAt.toLocaleString("en-US", opt) %></p>
            </div>
        <% } %>
    </ol>
</body>
</html>